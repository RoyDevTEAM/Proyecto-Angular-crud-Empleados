import { Component } from '@angular/core';  // Importa el módulo Component desde Angular Core

@Component({
  selector: 'app-dashboard',  // Define el selector para este componente
  templateUrl: './dashboard.component.html',  // Especifica la plantilla HTML asociada al componente
  styleUrls: ['./dashboard.component.scss']  // Especifica las hojas de estilo (CSS) asociadas al componente
})
export class DashboardComponent {  // Define la clase del componente
  // Esta clase no contiene ninguna lógica adicional, es un componente vacío
}
